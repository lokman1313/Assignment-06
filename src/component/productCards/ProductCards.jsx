import React, { use } from 'react';

const ProductCards = ({productDeteils}) => {
    const productDeteil=use(productDeteils);
    console.log(productDeteil)
    return (
        <div>
            
        </div>
    );
};

export default ProductCards;